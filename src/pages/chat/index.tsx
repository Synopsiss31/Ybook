import { Box, Typography } from '@mui/material';

const ChatPage = () => {
  return (
    <Box>
      <Typography variant="h4">Chat</Typography>
    </Box>
  );
};

export default ChatPage;
